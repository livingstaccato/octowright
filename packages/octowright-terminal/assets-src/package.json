{
  "name": "octowright-terminal-renderer",
  "private": true,
  "type": "module",
  "description": "Build-only project for octowright-terminal's dashboard renderer. NOT part of the root npm workspace and NOT a dependency of packages/octowright-frontend -- xterm must not re-enter core's bundle. Its only output is the committed src/octowright_terminal/assets/renderer.js.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run typecheck && node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@xterm/addon-unicode11": "^0.9.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^5.5.0"
  }
}
