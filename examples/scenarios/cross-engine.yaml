name: cross-engine
description: |
  One instance per engine — chromium, firefox, webkit — plus a fourth pane
  showing the live Octowright dashboard. The three players run the same
  showroom flow against the deterministic seed; the dashboard pane just
  watches the recording happen in real time.
participants:
  - persona: cx-chromium
    kind: chromium
    role: player
    url: about:blank
  - persona: cx-firefox
    kind: firefox
    role: player
    url: about:blank
  - persona: cx-webkit
    kind: webkit
    role: player
    url: about:blank
  - persona: cx-monitor
    kind: chromium
    role: monitor
    url: http://127.0.0.1:8765/
verify:
  player: "cross-engine-showroom"
